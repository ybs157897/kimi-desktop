---
"@moonshot-ai/kimi-desktop": minor
---

Overhaul the conversation timeline to a flat, borderless activity-row layout: consecutive tool calls now collapse into a single summary row that expands in place, prose and code blocks follow the Codex render spec, mermaid diagrams render as SVG, code headers show friendly language names, and collapsible sections animate open and closed.
