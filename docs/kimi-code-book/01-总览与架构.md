# 第 1 章 总览与架构

> 本章回答三个问题：Kimi Code 是什么、monorepo 如何组织、以及贯穿全部实现的设计主线是什么。

## 1.1 Kimi Code 是什么

**Kimi Code** 是 Moonshot AI（月之暗面）出品的终端 AI 编码代理，命令名 `kimi`。它在终端里运行，能够读改代码、执行 shell 命令、搜索文件、抓取网页，并基于反馈决定下一步。开箱即用 Moonshot 的 Kimi 模型，也支持其他兼容供应商。

与前一本书的主角（Grok Build，纯 Rust）不同，Kimi Code 是一个 **TypeScript / pnpm monorepo**（约 20 个 workspace 包、40 万行 TS），技术选型上有几个鲜明的产品决策：

- **单二进制分发**：用 Node.js 官方 **SEA（Single Executable Application）** + postject 打包，把 `node-pty` 等原生 `.node` 资产内嵌进二进制，一条命令安装、毫秒级启动 TUI。
- **自研 TUI 渲染库**：`pi-tui`，不用 ink/react，是一个"免依赖的终端图形栈"，为聊天界面高频流式更新量身定做。
- **双引擎架构**：`agent-core`（v1）与 `agent-core-v2`（DI × Scope 新架构）同仓并存，v2 已是默认引擎，`KIMI_CODE_LEGACY_FLAG=1` 可回退。
- **多宿主复用**：CLI（TUI）、VSCode 扩展（内嵌 SDK）、Web（kap-server）——"一个 SDK、三种宿主"。

### 1.1.1 运行形态

| 形态 | 触发方式 | 引擎 |
|---|---|---|
| 交互式 TUI | `kimi` | v2（默认）/ v1（legacy flag） |
| 非交互 | `kimi -p "..."` | v2 原生 print runner |
| Web | `kimi web` | kap-server（v2 引擎 + REST/WS + Web UI） |
| ACP | `kimi acp` | v2 原生 ACP 服务（legacy 走 acp-adapter） |
| VSCode | 扩展 | 内嵌 agent-core SDK（扩展进程内） |
| 可视化 | `kimi vis` | vis-server |

## 1.2 仓库布局

```
kimi-code/
├── apps/
│   ├── kimi-code/        # CLI/TUI 主应用（5.5 万行，最终产品）
│   ├── kimi-inspect/     # kap-server /api/v1/debug 反射面的 Web 调试台
│   ├── vis/              # 会话/回放的可视化调试工具
│   └── vscode/           # "Kimi Code for VS Code" 扩展
├── packages/
│   ├── agent-core/       # v1 引擎（6.9 万行）
│   ├── agent-core-v2/    # v2 引擎，DI × Scope 新架构（10.6 万行，当前主架构）
│   ├── kap-server/       # Kimi Code 服务器（REST + WS），v2 引擎的传输外壳
│   ├── klient/           # 契约驱动客户端 SDK（global/session/agent 面，zod 校验）
│   ├── node-sdk/         # 公共 TS SDK 与 KimiHarness 门面
│   ├── kosong/           # LLM/供应商抽象层（"空"——最薄的 LLM 抽象）
│   ├── kaos/             # 执行环境与文件/进程抽象（"Kimi Agent OS"）
│   ├── oauth/            # Kimi OAuth 与托管认证
│   ├── minidb/           # 自研嵌入式 JSON 文档数据库（1.8 万行）
│   ├── pi-tui/           # 自研 TUI 渲染库
│   ├── protocol/         # 协议线类型（v1 时代的 RPC 契约）
│   ├── transcript/       # 会话转录渲染数据层（幂等 op + 订阅粒度）
│   ├── acp-adapter/      # ACP（Agent Client Protocol）v1 适配
│   ├── acp-server/       # ACP v2 适配服务器
│   ├── telemetry/        # 客户端遥测基础设施
│   ├── tree-sitter-bash/ # 纯 TS bash 解析器（无 wasm、预算封顶）
│   ├── migration-legacy/ # 旧版（~/.kimi）迁移逻辑
│   └── kaos/             # 执行环境抽象
├── plugins/              # 官方插件（marketplace.json + official/）
└── docs/                 # VitePress 文档站（en/zh）
```

**代码规模 Top 10（src/ 行数）**：agent-core-v2（10.6 万）、agent-core（6.9 万）、apps/kimi-code（5.5 万）、kap-server（2.9 万）、minidb（1.8 万）、kimi-inspect（1.2 万）、pi-tui（1.2 万）、protocol（1 万）、kosong（9 千）、node-sdk（7 千）。

## 1.3 总体架构：壳-引擎严格分层

```
┌──────────────────────────────────────────────────────────────┐
│ apps/kimi-code（CLI/TUI 外壳）                                  │
│  参数解析 + 进程生命周期 + 终端 UI + 更新/迁移/反馈              │
│  ★ 不直接 import agent-core（AGENTS.md 硬约束）                 │
└──────────────────────────┬───────────────────────────────────┘
                           │ 唯一入口：@moonshot-ai/kimi-code-sdk
                           │ （KimiHarness / Session 门面）
┌──────────────────────────▼───────────────────────────────────┐
│ packages/node-sdk（SDK）                                       │
│  v1: createKimiHarness() → SDKRpcClient（进程内 RPC）          │
│  v2: createKimiHarnessV2() → bootstrap() + klient memory 传输 │
└──────────────────────────┬───────────────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────────────┐
│ packages/agent-core-v2（引擎）    packages/agent-core（v1）    │
│  DI × Scope 四层作用域树           Agent/Session 门面 + RPC    │
│  App/Workspace/Session/Agent       （v2 的前传，WIP port 源）  │
└──────────────────────────┬───────────────────────────────────┘
                           │
      ┌────────────────────┼────────────────────┐
      ▼                    ▼                    ▼
 kap-server（REST/WS）  klient（客户端 SDK）  acp-server（ACP 适配）
      │                    │                    │
 apps/kimi-inspect     apps/vscode（内嵌）    IDE/宿主 ACP 客户端
```

**三条架构主线**：

1. **壳-引擎分层极干净**：CLI/TUI 不碰引擎，一切能力经 SDK 门面；v1→v2 引擎切换只改一个工厂函数（`createKimiHarnessV2`）+ 一个环境变量（`KIMI_CODE_LEGACY_FLAG`），TUI 层对引擎版本无感知。
2. **作用域即架构（v2）**：`App → Workspace → Session → Agent` 四层 `LifecycleScope` 作用域树是 v2 的第一公民——资源拥有者决定生命周期，所有能力都是可挂账本、可撤回、可内省的 DI 单元。kap-server 甚至直接反射整个 DI 注册表做 RPC（"注册的服务就是公共契约"）。
3. **一切可重放状态走 wire journal**：每 Agent 一个 `wire.jsonl` 追加日志 + 纯函数 Op/Model，undo、压缩、续会话、崩溃恢复全部是对日志的确定性重放。这是两代引擎共享的持久化哲学。

## 1.4 设计主线（阅读线索）

| 主线 | 体现 |
|---|---|
| 引擎可替换 | SDK 门面 + `createKimiHarnessV2` + legacy flag；node-sdk 的 v1/v2 双实现 |
| 可重放状态 | wire.jsonl 事件溯源、resume 逐条重放、fork 日志切片 |
| 权限是第一公民 | v2 的 12 策略静态链 + veto 事件链 + 三层工具可见性 |
| 终端完整性 | stty 保存/恢复、SEA 资产解包、headless 退出双保险、差分渲染 |
| 确定性 & 预算 | tree-sitter-bash 预算封顶解析、minidb 的 LWW 恢复、更新灰度确定性分桶 |
| 防御性工程 | 401 竞态恢复、跨进程锁、torn 行容忍、sticky failure——每个边界都有事故注释 |

## 1.5 阅读路线图

| 章节 | 内容 |
|---|---|
| 第 2 章 | CLI 应用外壳：启动时序、更新系统、SEA 打包、进程生命周期 |
| 第 3 章 | v2 引擎：DI Scope 内核、回合主循环、工具/权限、wire 日志、压缩 |
| 第 4 章 | v1 引擎：手写 DI 容器、无状态纯循环、事件溯源（v2 的前传） |
| 第 5 章 | KAP 服务器与 kosong：REST/WS 传输外壳、事件流、LLM 抽象 |
| 第 6 章 | TUI 与 VSCode：pi-tui 差分渲染、三宿主复用、双许可审批 |
| 第 7 章 | 协议层：klient 契约、node-sdk 兼容层、ACP 两代实现、transcript |
| 第 8 章 | minidb：WAL + 组提交、zskiplist 索引、FTS、多进程分片 |
| 第 9 章 | 基础设施：oauth、kaos、tree-sitter-bash、telemetry、kimi-inspect |

每章统一按"定位 → 结构 → 数据流 → 实现细节 → 代码索引"组织，代码引用格式为 `packages/<pkg>/src/<path>.ts:行号`（或 `apps/<app>/src/...`），全部相对仓库根 `kimi-code/` 解析。术语沿用官方文档约定：轮次（turn）、工具调用（tool call）、审批请求（approval request）、YOLO 模式等。
